{
  "name": "sehat-sathi",
  "version": "1.0.0",
  "description": "AI-powered medical triage system with voice capabilities",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "concurrently \"npm --prefix api run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix api run build && npm --prefix frontend run build",
    "start": "npm --prefix api start"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.86.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "nodemailer": "^7.0.11"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}
